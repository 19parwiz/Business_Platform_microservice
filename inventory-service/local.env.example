# Copy to local.env (same folder). Not committed: local.env is gitignored.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=flower_shop
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SSL_MODE=disable

GRPC_PORT=50052
HTTP_PORT=8082

# Kafka broker (host port when using docker-compose kafka on this machine)
BROKERS=localhost:9092
